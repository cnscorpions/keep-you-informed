{
  "scripts": {
    "start": "node src/index.js",
    "git": "git add . && git commit -m",
    "postgit": "git pull && git push && git status"
  },
  "devDependencies": {
    "@slack/webhook": "^6.1.0",
    "cheerio": "^1.0.0-rc.6",
    "ding-robot": "^1.0.0",
    "node-fetch": "^2.6.1",
    "node-schedule": "^2.0.0"
  }
}
